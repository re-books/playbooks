- hosts: master
  become: true
  connection: local
  vars:
    allow_world_readable_tmpfiles: true

  roles:
    - install-nginx
    - install-certbot
    - add-www-user
    - install-php
    - install-mysql
    - create-database-user
    - create-database
    - create-host
    - install-app
    #- init-pilot